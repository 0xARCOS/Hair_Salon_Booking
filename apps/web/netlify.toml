[build]
  base = "apps/web"
  command = "cd ../.. && npx turbo run build --filter=web"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
